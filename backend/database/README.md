# Database Configuration Module (/database)

## Purpose
Manages connection pooling, initial connection, and database event listeners for MongoDB.

## Files to Create (Placeholder)
- connection.js: Mongoose connection initialization.
- seed.js: Initial database seeding script (creates Default Admin & Receptionist accounts/roles).

## Guidelines
- Implement auto-reconnection logic.
- Log connection events (connected, error, disconnected).
