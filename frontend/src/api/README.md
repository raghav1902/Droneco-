# API Base Layer (/src/api)

## Purpose
Configures the HTTP client library (Axios) to communicate with the Node.js backend.

## Files to Create (Placeholder)
- xiosClient.js: Configures baseURL, headers, and request/response interceptors to automatically attach JWT token and handle 401 unauthorized errors globally.
