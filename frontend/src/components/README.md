# Reusable UI Components (/src/components)

## Purpose
Shared elements used across pages and forms. These must remain pure presentation components without business logic.

## Examples
- Buttons, TextFields, SelectMenus, Checkboxes.
- Stepper indicator (visual progress bar for Step 1 -> 2 -> 3).
- Cards, Modals, Loader animations.
