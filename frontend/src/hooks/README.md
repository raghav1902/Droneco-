# Custom React Hooks (/src/hooks)

## Purpose
Contains custom reusable React hooks to share stateful logic.

## Examples (Placeholder)
- useAuth.js: Syntactic sugar to access AuthContext.
- useLocalStorage.js: Helper hook to persist user session data locally.
- useFormWizard.js: Manages stepping state and step-level history tracking.
