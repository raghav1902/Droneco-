# API Services Layer (/src/services)

## Purpose
Abstraits data fetching from component logic. Each API file should mirror backend endpoints.

## Files to Create (Placeholder)
- uthService.js: Methods for login, logout, and refreshing session tokens.
- leadService.js: Methods for submitting lead form, fetching leads list (role-filtered), and updating lead status.
- userService.js: Admin options to manage receptionist accounts.
