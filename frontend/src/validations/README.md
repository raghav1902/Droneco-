# Client Validation Schemas (/src/validations)

## Purpose
Uses schema parsing (e.g., Zod, Yup) to validate inputs on the client-side before forwarding data to API services.

## Files to Create (Placeholder)
- step1.schema.js: Validates name lengths, email structures, and mobile formats.
- step2.schema.js: Validates selected options.
- step3.schema.js: Validates queries and remarks lengths.
